spring:
  cloud:
    zookeeper:
      config:
        enabled: true
        root: config
        defaultContext: apps
#        profileSeparator: '-'
      connect-string: localhost:2181
  application:
    name: zk-config-client

